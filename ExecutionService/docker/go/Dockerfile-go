# Dockerfile for Go Code Execution
FROM golang:1.21-slim

WORKDIR /app

# Copy the Go execution script
COPY execute.sh /app/execute.sh
RUN chmod +x /app/execute.sh

# Install Go dependencies if needed (you can customize this)
RUN apt-get update && apt-get install -y gcc

ENTRYPOINT ["/app/execute.sh"]
